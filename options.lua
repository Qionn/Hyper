newoption {
	trigger = "renderer",
	description = "Choose a renderer to use in the backend",
	default = "vulkan",
	category = "Hyper",
	allowed = {
		{ "vulkan", "Vulkan (LunarG SDK required)" }
	}
}
